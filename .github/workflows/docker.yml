name: Build and Deploy to Docker Hub

on:
  - push
  - pull_request

jobs:
  test:
    name: Setup, Build, Test
    uses: brianmay/workflows/.github/workflows/elixir.yaml@b8ac8e58821a896616de972c4e8602c3e17a6a46

  push:
    name: Push
    uses: brianmay/workflows/.github/workflows/docker.yaml@b8ac8e58821a896616de972c4e8602c3e17a6a46
    secrets:
      DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
    needs:
      - test
